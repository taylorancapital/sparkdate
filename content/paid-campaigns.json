{
  "_comment": "Which live Meta campaigns the budget ladder drives, and which ones it is deliberately leaving alone. Read by scripts/budget-ladder.js; written by hand. This file exists because the ladder used to hold its campaign list as a hardcoded array in scripts/meta-budget-ladder.js, so a second event was a code edit -- and the edit nobody made was invisible, because a campaign missing from the array is simply never laddered. Adding an October event is now: add the event to brand.json, build the campaign, add four lines here.",

  "_how_to_add": [
    "1. The event must exist in content/brand.json under `events` -- the ladder reads its DATE from there and refuses a key it cannot find. Nothing about the ladder duplicates the event.",
    "2. Decide legacy or v2 (see _fields.playbook below -- v2 is the current playbook for any event that doesn't already have a live campaign built on the legacy shape).",
    "   LEGACY (one campaign): build it (paused is fine) and take its numeric id from Ads Manager or `node scripts/meta-ads-review.js`. Add one entry: event, campaign_id, name, total (whole-run dollars), runway_start.",
    "   V2 (two campaigns): build BOTH `<Event> | Cold` and `<Event> | Retargeting`, paused, per content/brand.json paid_template.playbook_v2. Add TWO entries, one per role, sharing the SAME `total` and `runway_start` -- each carries `playbook: \"v2\"` and `role: \"cold\"` or `\"retargeting\"`. Do not set `share`; v2 does not use it (see _fields.playbook).",
    "3. `node scripts/meta-budget-ladder.js --all` (dry run) and read the plan. `--forecast=45` shows what the account spends per day once the runs overlap.",
    "4. Nothing else. The 03:00 `SparkDate Budget Ladder` scheduled task already runs `--all --execute`."
  ],

  "_fields": {
    "event": "Key in content/brand.json events. The ladder gets the event date from there and nowhere else.",
    "campaign_id": "Numeric Meta campaign id. Legacy must be a CBO campaign -- the ladder sets a campaign-level daily_budget and refuses one that has none. v2 campaigns are ABO (see playbook_v2.campaign.budget); each of the two still gets its own daily_budget set directly.",
    "name": "For output only. The live name is read back from Meta and printed next to it.",
    "total": "Whole-run budget in dollars, same meaning for both playbooks. Legacy: distributed across phases by percentage. v2: scales every phase's fixed per-day rate by total/296 (296 is the playbook's own reference event) -- a v2 event's cold AND retargeting entries both carry the SAME total, since it describes one shared run, not two independent budgets. Under $200 on legacy, prime falls below Meta's $2.00/day floor; validation warns. v2 has its own floor handling per phase (playbook_v2._floor_priority_rule) and does not need this warning.",
    "runway_start": "First day of spend. Legacy: must leave at least 18 days before the event (brand.json runway._floor) and ideally 21 (runway.minimum_days) -- prime literally disappears below the floor. v2: no hard floor -- a short runway gracefully starts later in the phase sequence instead (playbook_v2._lead_time.cold_start_rule), down to just Close's own window.",
    "playbook": "Optional, default legacy (i.e. omit this field). \"v2\" reads content/brand.json paid_template.playbook_v2 instead of the legacy campaign/ad_sets/phases -- see that block's own _comment. Existing entries (Loxleys) must never set this; its live campaign runs on legacy and changing this field would silently recompute a different rate for money already spending.",
    "role": "REQUIRED when playbook is \"v2\", and meaningless otherwise. \"cold\" or \"retargeting\" -- which of the event's two v2 campaigns this entry drives. One of each per event.",
    "share": "Legacy only. Optional, default 1. The fraction of `total` THIS campaign carries, for a legacy event split across more than one campaign. Shares for one event may not sum above 1. v2 does NOT use this field -- its cold:retarget split varies BY PHASE (80/20 in Seed, 60/40 in Build, 35/65 in Close), which a single flat fraction cannot express; use `role` instead.",
    "managed": "Optional, default true. false parks an entry without deleting the numbers -- it keeps its id out of the ungoverned report while the ladder leaves it alone.",
    "note": "Why this entry is the way it is."
  },

  "guards": {
    "max_daily_dollars": 25,
    "max_account_daily_dollars": 40,
    "_why_account": "Each ladder is individually sane at $11.57/day; four at once is the number nobody was watching. reports/OCTOBER_SLATE_FEASIBILITY_2026-09-03.md models the October slate peaking at $27.50-$32.50/day across four campaigns, and the account's real record is $24.67/day across the seven days to Sep 1. $40 clears the modelled peak and still catches a doubling. A breach refuses the WHOLE run rather than laddering some campaigns and not others.",
    "_why_campaign": "A ceiling, not a jump limit. The ladder's own prime -> convert step is 4.5x by design, so any jump guard tight enough to catch a typo also blocks the real steps. What needs catching is a fat-fingered total or a wrong event date, and both show up as an absurd daily figure."
  },

  "campaigns": [
    {
      "event": "LX",
      "campaign_id": "120251304238920542",
      "name": "Loxleys | Sales",
      "total": 180,
      "runway_start": "2026-08-23",
      "note": "The $180 total is Taylor's $3.00/day prime choice on the original Traffic campaign (memory lx-campaign-live), carried onto the Sales campaign that replaced it on 09-05. Prime lands at $1.80 and is floored to Meta's $2.00. Retire this entry after 2026-09-22."
    }
  ],

  "acknowledged": [
    {
      "campaign_id": "120251305428200542",
      "name": "Marion Court | Sales",
      "why": "Hand-set to $14.00/day by Taylor. Built 09-05 as a T-3 hail mary on the 2-for-1 female creative; its budget is a deliberate flat push, not a ladder. Handing it to the ladder would pull it DOWN to the close-phase rate two days before the event.",
      "review_after": "2026-09-08"
    },
    {
      "campaign_id": "120250958681350542",
      "name": "Marion Court Retargeting",
      "why": "Left untouched on purpose (HANDOFF 09-05): MC-RT-QUANG and MC-RT-NO-SCORECARDS stay running for the data. $6/day, ends with the event.",
      "review_after": "2026-09-08"
    }
  ],

  "_acknowledged_note": "A campaign here is one that is ACTIVE and deliberately outside the ladder. Anything ACTIVE in the account that is in neither list is reported as UNGOVERNED and exits non-zero -- that is the whole point of the file. `review_after` makes an acknowledgement expire instead of muting a campaign forever.",

  "_playbook_ref": "reports/ADS_OBJECTIVE_GAP_ANALYSIS_2026-09-06.md section 8 is the current spec for a future event's phase structure (Seed/Build/Close), its cold:retarget split per phase, and its zero-gender-ad-set rule -- built and offline-tested 2026-09-06 as content/brand.json paid_template.playbook_v2 and scripts/budget-ladder.js's roleRates()/phaseWindowsV2()/rateFor() dispatch (tests/budget-ladder.test.js, 22 cases). CORRECTION to what this note said when first written: it claimed the `share` field already covered a Cold+Retargeting split with no arithmetic change needed. That was wrong -- `share` is one flat fraction applied to every phase alike, and the playbook's cold:retarget ratio VARIES by phase (80/20 -> 60/40 -> 35/65), which a flat fraction cannot express. v2 entries use `role` (\"cold\"/\"retargeting\") instead of `share` -- see _fields.playbook and _fields.role above. What's still NOT done: scripts/build-paid-campaign.js's --execute still only builds the legacy single-campaign shape -- it calls the live Marketing API with no offline test coverage, and rewriting its campaign-creation logic is its own focused pass, same reasoning as when this note was first written. Loxleys' single entry above stays on legacy and was not restructured into two v2 campaigns -- deliberate: rebuilding a LIVE campaign's structure this close to its 2026-09-22 event risks a cold-start at the worst time, per playbook_v2._lead_time's own compressed-schedule rule, and legacy vs v2 is exactly the dispatch this file's `playbook` field exists to make explicit rather than implicit."
}
