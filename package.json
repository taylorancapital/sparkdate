{
  "name": "sparkdate",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:ci": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "firebase": "^10.7.0",
    "firebase-admin": "^12.0.0",
    "resend": "^3.0.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
